{
  "name": "opencode-chat",
  "displayName": "OpenCode Chat",
  "description": "GitHub Copilot-like chat using OpenCode as backend",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Machine Learning",
    "Chat"
  ],
  "keywords": [
    "ai",
    "chat",
    "copilot",
    "opencode"
  ],
  "activationEvents": [
    "onView:opencode.chatView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "opencodespec.openChat",
        "title": "Open Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "opencodespec.sendMessage",
        "title": "Send Message to Chat"
      },
      {
        "command": "opencodespec.helloWorld",
        "title": "Hello World"
      }
    ],
    "views": {
      "opencodeChat": [
        {
          "type": "webview",
          "id": "opencode.chatView",
          "name": "Chat",
          "when": "true"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "opencodeChat",
          "title": "OpenCode Chat",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "menus": {
      "commandPalette": [
        {
          "command": "opencodespec.sendMessage",
          "when": "false"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  }
}
